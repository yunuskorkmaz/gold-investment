import theme from './theme';

const colors = {
  pageBg: '#f1f1f1',
  listItemBg: '#fff',
  line: '#ccc',
  primaryText: '#000',
  secondaryText: '#666',
  current: '#777',
  detailBgUp: 'green',
  detailBgDown: 'red',
  detailText: '#fff',
  headerBg: '#fff',
};

export default {
  ...theme,
  colors,
};
