import theme from './theme';

const colors = {
  pageBg: '#f1f1f1',
  listItemBg: '#fff',
  line: '#ccc',
  primaryText: '#000',
  secondaryText: '#666',
  current: '#777',
};

export default {
  ...theme,
  colors,
};
